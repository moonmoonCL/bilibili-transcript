# Bilibili Transcript 环境变量配置

# Chrome 调试端口（用于 CDP 兜底方案）
CHROME_DEBUG_PORT=9222

# yt-dlp 配置
# 如果需要使用自定义 cookies 文件，可以取消注释以下行
# YT_DLP_COOKIES=/path/to/cookies.txt

# 日志级别（可选）
# LOG_LEVEL=info

# 临时文件目录（可选）
# TEMP_DIR=/tmp/bilibili-transcript